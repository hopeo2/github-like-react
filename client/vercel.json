{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "headers": [
        { "key": "Access-Control-Allow-Credentials", "value": "true" }
    ]
}